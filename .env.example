# FleetWatch environment — copy to .env.local (Next) / .env (worker).
# NEVER commit real secrets. Values here are placeholders only.

NODE_ENV=development
LOG_LEVEL=info

# --- added in later phases (kept here as documentation) ---
# Fase 1 — database
# DATABASE_URL=postgresql://fleetwatch:fleetwatch@localhost:5432/fleetwatch
# Fase 2 — secret envelope encryption (32-byte base64 key; generate per env)
# SECRET_ENC_KEY=
# Fase 7 — queue
# REDIS_URL=redis://localhost:6379
